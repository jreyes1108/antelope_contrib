BIN = rtmweb
MAN1 = rtmweb.1
PF = rtmweb.pf

DATADIR = python/rtmweb

DATA = rtmweb_javascript.js

CLEAN = *.pyc

ANTELOPEMAKELOCAL = $(ANTELOPE)/local/include/antelopemake.local

include $(ANTELOPEMAKE)
include $(ANTELOPEMAKELOCAL)

SUBDIR= /local
